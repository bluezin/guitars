<section class="container-general">
  <div style="margin-top: 20px">
    <%= link_to t("common.back"), products_path, class: "button-primary" %>
  </div>

  <%= render "form", product: @product, categories: Category.all %>
</section>
